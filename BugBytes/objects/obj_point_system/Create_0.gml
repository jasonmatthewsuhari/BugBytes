/// @description Initialise point system

x_offset = room_get_viewport(room, 0)[3] / 2 - 50;			// x offset amount from camera
y_offset = room_get_viewport(room, 0)[4] / 2 - 30;			// y offset amount from camera

x_pos = x;
y_pos = y;